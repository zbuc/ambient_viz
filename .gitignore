# macOS Finder metadata
.DS_Store

# Python bytecode
__pycache__/
*.py[cod]

# Node dependencies
node_modules/

# Claude Code session / job state
.claude/

# raw audio
AMBIENT.RAW
# orrery bus boot-epoch counter (per-node runtime state)
server/.orrery-boot-epoch

# proto codegen intermediates (generated JS in server/src/gen IS committed)
proto/.gen-ts/
proto/node_modules/

# replay-harness and graph-simulator run outputs (large, regenerable from the golden)
projects/pain-material/fixtures/*/replays/
projects/pain-material/fixtures/*/sims/
