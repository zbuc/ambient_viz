# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "infinitedsp-core"
version = "1.1.0"
authors = ["Fredrik Andersson <fredrikandersson@mac.com>"]
build = false
exclude = [
    "assets/audio/*",
    ".idea/*",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A modular audio DSP library for synthesis and effects."
readme = "README.md"
keywords = [
    "audio",
    "dsp",
    "synthesis",
    "effects",
    "no_std",
]
categories = [
    "multimedia::audio",
    "embedded",
    "no-std",
]
license = "MIT"
repository = "https://github.com/Na1w/infinitedsp/"

[features]
debug_visualize = []
default = []
# Opt-in: swap exact libm transcendentals (tan/sin/log/exp) in the per-sample
# DSP hot paths for cheap polynomial/bit-trick approximations (upstream PRs
# #46/#47/#48). Large speedups on the Daisy's Cortex-M7 (no hardware
# transcendental unit). Our build enables this — see crates/dsp/Cargo.toml.
perf-approximations = []

[lib]
name = "infinitedsp_core"
path = "src/lib.rs"

[[bench]]
name = "dsp_benchmarks"
path = "benches/dsp_benchmarks.rs"
harness = false

[dependencies.libm]
version = "0.2"

[dependencies.microfft]
version = "0.6.0"

[dependencies.num-complex]
version = "0.4"
features = ["libm"]
default-features = false

[dependencies.wide]
version = "1.1.1"
default-features = false

[dev-dependencies.iai-callgrind]
version = "0.16.1"
