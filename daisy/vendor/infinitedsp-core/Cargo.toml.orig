[package]
name = "infinitedsp-core"
authors = ["Fredrik Andersson <fredrikandersson@mac.com>"]
version = "1.1.0"

edition = "2021"
license = "MIT"
description = "A modular audio DSP library for synthesis and effects."
repository = "https://github.com/Na1w/infinitedsp/"
readme = "README.md"
keywords = ["audio", "dsp", "synthesis", "effects", "no_std"]
categories = ["multimedia::audio", "embedded", "no-std"]
exclude = ["assets/audio/*", ".idea/*"]

[workspace]
members = [
    ".",
    "examples_app",
]

[features]
default = []
debug_visualize = []
# Opt-in: swap exact libm transcendentals (tan/sin/log/exp) in the per-sample
# DSP hot paths for cheap polynomial/bit-trick approximations. Large speedups on
# targets without hardware transcendental units (e.g. Cortex-M7), at the cost of
# small, bounded error. Off by default — the default build is bit-exact.
perf-approximations = []

[dependencies]
wide = { version = "1.1.1", default-features = false }
microfft = "0.6.0"
num-complex = { version = "0.4", default-features = false, features = ["libm"] }
libm = "0.2"

[dev-dependencies]
iai-callgrind = "0.16.1"

[[bench]]
name = "dsp_benchmarks"
harness = false

[profile.release]
lto = true
opt-level = 3
