"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v6.33.4
// source: bus.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.Bundle_Item = exports.Bundle = exports.Event = exports.State = exports.SignalPacket = exports.TimePoint_RenderFrame = exports.TimePoint_Wall = exports.TimePoint_Monotonic = exports.TimePoint_Musical = exports.TimePoint_AudioSample = exports.TimePoint = exports.Source = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const common_1 = require("./common");
exports.protobufPackage = "orrery.bus.v1";
function createBaseSource() {
    return { sourceId: "", seq: 0, sig: undefined, certFingerprint: undefined, bootEpoch: 0 };
}
exports.Source = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.sourceId !== "") {
            writer.uint32(10).string(message.sourceId);
        }
        if (message.seq !== 0) {
            writer.uint32(16).uint64(message.seq);
        }
        if (message.sig !== undefined) {
            writer.uint32(26).bytes(message.sig);
        }
        if (message.certFingerprint !== undefined) {
            writer.uint32(34).string(message.certFingerprint);
        }
        if (message.bootEpoch !== 0) {
            writer.uint32(40).uint32(message.bootEpoch);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSource();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.sourceId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.seq = longToNumber(reader.uint64());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.sig = reader.bytes();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.certFingerprint = reader.string();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.bootEpoch = reader.uint32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            sourceId: isSet(object.sourceId)
                ? globalThis.String(object.sourceId)
                : isSet(object.source_id)
                    ? globalThis.String(object.source_id)
                    : "",
            seq: isSet(object.seq) ? globalThis.Number(object.seq) : 0,
            sig: isSet(object.sig) ? bytesFromBase64(object.sig) : undefined,
            certFingerprint: isSet(object.certFingerprint)
                ? globalThis.String(object.certFingerprint)
                : isSet(object.cert_fingerprint)
                    ? globalThis.String(object.cert_fingerprint)
                    : undefined,
            bootEpoch: isSet(object.bootEpoch)
                ? globalThis.Number(object.bootEpoch)
                : isSet(object.boot_epoch)
                    ? globalThis.Number(object.boot_epoch)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.sourceId !== "") {
            obj.sourceId = message.sourceId;
        }
        if (message.seq !== 0) {
            obj.seq = Math.round(message.seq);
        }
        if (message.sig !== undefined) {
            obj.sig = base64FromBytes(message.sig);
        }
        if (message.certFingerprint !== undefined) {
            obj.certFingerprint = message.certFingerprint;
        }
        if (message.bootEpoch !== 0) {
            obj.bootEpoch = Math.round(message.bootEpoch);
        }
        return obj;
    },
    create(base) {
        return exports.Source.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSource();
        message.sourceId = object.sourceId ?? "";
        message.seq = object.seq ?? 0;
        message.sig = object.sig ?? undefined;
        message.certFingerprint = object.certFingerprint ?? undefined;
        message.bootEpoch = object.bootEpoch ?? 0;
        return message;
    },
};
function createBaseTimePoint() {
    return { audioSample: undefined, musical: undefined, monotonic: undefined, wall: undefined, renderFrame: undefined };
}
exports.TimePoint = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.audioSample !== undefined) {
            exports.TimePoint_AudioSample.encode(message.audioSample, writer.uint32(10).fork()).join();
        }
        if (message.musical !== undefined) {
            exports.TimePoint_Musical.encode(message.musical, writer.uint32(18).fork()).join();
        }
        if (message.monotonic !== undefined) {
            exports.TimePoint_Monotonic.encode(message.monotonic, writer.uint32(26).fork()).join();
        }
        if (message.wall !== undefined) {
            exports.TimePoint_Wall.encode(message.wall, writer.uint32(34).fork()).join();
        }
        if (message.renderFrame !== undefined) {
            exports.TimePoint_RenderFrame.encode(message.renderFrame, writer.uint32(42).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseTimePoint();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.audioSample = exports.TimePoint_AudioSample.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.musical = exports.TimePoint_Musical.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.monotonic = exports.TimePoint_Monotonic.decode(reader, reader.uint32());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.wall = exports.TimePoint_Wall.decode(reader, reader.uint32());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.renderFrame = exports.TimePoint_RenderFrame.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            audioSample: isSet(object.audioSample)
                ? exports.TimePoint_AudioSample.fromJSON(object.audioSample)
                : isSet(object.audio_sample)
                    ? exports.TimePoint_AudioSample.fromJSON(object.audio_sample)
                    : undefined,
            musical: isSet(object.musical) ? exports.TimePoint_Musical.fromJSON(object.musical) : undefined,
            monotonic: isSet(object.monotonic) ? exports.TimePoint_Monotonic.fromJSON(object.monotonic) : undefined,
            wall: isSet(object.wall) ? exports.TimePoint_Wall.fromJSON(object.wall) : undefined,
            renderFrame: isSet(object.renderFrame)
                ? exports.TimePoint_RenderFrame.fromJSON(object.renderFrame)
                : isSet(object.render_frame)
                    ? exports.TimePoint_RenderFrame.fromJSON(object.render_frame)
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.audioSample !== undefined) {
            obj.audioSample = exports.TimePoint_AudioSample.toJSON(message.audioSample);
        }
        if (message.musical !== undefined) {
            obj.musical = exports.TimePoint_Musical.toJSON(message.musical);
        }
        if (message.monotonic !== undefined) {
            obj.monotonic = exports.TimePoint_Monotonic.toJSON(message.monotonic);
        }
        if (message.wall !== undefined) {
            obj.wall = exports.TimePoint_Wall.toJSON(message.wall);
        }
        if (message.renderFrame !== undefined) {
            obj.renderFrame = exports.TimePoint_RenderFrame.toJSON(message.renderFrame);
        }
        return obj;
    },
    create(base) {
        return exports.TimePoint.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseTimePoint();
        message.audioSample = (object.audioSample !== undefined && object.audioSample !== null)
            ? exports.TimePoint_AudioSample.fromPartial(object.audioSample)
            : undefined;
        message.musical = (object.musical !== undefined && object.musical !== null)
            ? exports.TimePoint_Musical.fromPartial(object.musical)
            : undefined;
        message.monotonic = (object.monotonic !== undefined && object.monotonic !== null)
            ? exports.TimePoint_Monotonic.fromPartial(object.monotonic)
            : undefined;
        message.wall = (object.wall !== undefined && object.wall !== null)
            ? exports.TimePoint_Wall.fromPartial(object.wall)
            : undefined;
        message.renderFrame = (object.renderFrame !== undefined && object.renderFrame !== null)
            ? exports.TimePoint_RenderFrame.fromPartial(object.renderFrame)
            : undefined;
        return message;
    },
};
function createBaseTimePoint_AudioSample() {
    return { device: "", sample: 0, sampleRate: 0 };
}
exports.TimePoint_AudioSample = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.device !== "") {
            writer.uint32(10).string(message.device);
        }
        if (message.sample !== 0) {
            writer.uint32(16).uint64(message.sample);
        }
        if (message.sampleRate !== 0) {
            writer.uint32(24).uint32(message.sampleRate);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseTimePoint_AudioSample();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.device = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.sample = longToNumber(reader.uint64());
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.sampleRate = reader.uint32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            device: isSet(object.device) ? globalThis.String(object.device) : "",
            sample: isSet(object.sample) ? globalThis.Number(object.sample) : 0,
            sampleRate: isSet(object.sampleRate)
                ? globalThis.Number(object.sampleRate)
                : isSet(object.sample_rate)
                    ? globalThis.Number(object.sample_rate)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.device !== "") {
            obj.device = message.device;
        }
        if (message.sample !== 0) {
            obj.sample = Math.round(message.sample);
        }
        if (message.sampleRate !== 0) {
            obj.sampleRate = Math.round(message.sampleRate);
        }
        return obj;
    },
    create(base) {
        return exports.TimePoint_AudioSample.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseTimePoint_AudioSample();
        message.device = object.device ?? "";
        message.sample = object.sample ?? 0;
        message.sampleRate = object.sampleRate ?? 0;
        return message;
    },
};
function createBaseTimePoint_Musical() {
    return { beat: 0, bar: undefined, tempo: undefined };
}
exports.TimePoint_Musical = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.beat !== 0) {
            writer.uint32(9).double(message.beat);
        }
        if (message.bar !== undefined) {
            writer.uint32(16).uint32(message.bar);
        }
        if (message.tempo !== undefined) {
            writer.uint32(25).double(message.tempo);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseTimePoint_Musical();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 9) {
                        break;
                    }
                    message.beat = reader.double();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.bar = reader.uint32();
                    continue;
                }
                case 3: {
                    if (tag !== 25) {
                        break;
                    }
                    message.tempo = reader.double();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            beat: isSet(object.beat) ? globalThis.Number(object.beat) : 0,
            bar: isSet(object.bar) ? globalThis.Number(object.bar) : undefined,
            tempo: isSet(object.tempo) ? globalThis.Number(object.tempo) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.beat !== 0) {
            obj.beat = message.beat;
        }
        if (message.bar !== undefined) {
            obj.bar = Math.round(message.bar);
        }
        if (message.tempo !== undefined) {
            obj.tempo = message.tempo;
        }
        return obj;
    },
    create(base) {
        return exports.TimePoint_Musical.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseTimePoint_Musical();
        message.beat = object.beat ?? 0;
        message.bar = object.bar ?? undefined;
        message.tempo = object.tempo ?? undefined;
        return message;
    },
};
function createBaseTimePoint_Monotonic() {
    return { device: "", nanos: 0 };
}
exports.TimePoint_Monotonic = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.device !== "") {
            writer.uint32(10).string(message.device);
        }
        if (message.nanos !== 0) {
            writer.uint32(16).uint64(message.nanos);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseTimePoint_Monotonic();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.device = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.nanos = longToNumber(reader.uint64());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            device: isSet(object.device) ? globalThis.String(object.device) : "",
            nanos: isSet(object.nanos) ? globalThis.Number(object.nanos) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.device !== "") {
            obj.device = message.device;
        }
        if (message.nanos !== 0) {
            obj.nanos = Math.round(message.nanos);
        }
        return obj;
    },
    create(base) {
        return exports.TimePoint_Monotonic.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseTimePoint_Monotonic();
        message.device = object.device ?? "";
        message.nanos = object.nanos ?? 0;
        return message;
    },
};
function createBaseTimePoint_Wall() {
    return { unixNanos: 0 };
}
exports.TimePoint_Wall = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.unixNanos !== 0) {
            writer.uint32(8).uint64(message.unixNanos);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseTimePoint_Wall();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.unixNanos = longToNumber(reader.uint64());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            unixNanos: isSet(object.unixNanos)
                ? globalThis.Number(object.unixNanos)
                : isSet(object.unix_nanos)
                    ? globalThis.Number(object.unix_nanos)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.unixNanos !== 0) {
            obj.unixNanos = Math.round(message.unixNanos);
        }
        return obj;
    },
    create(base) {
        return exports.TimePoint_Wall.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseTimePoint_Wall();
        message.unixNanos = object.unixNanos ?? 0;
        return message;
    },
};
function createBaseTimePoint_RenderFrame() {
    return { group: "", frame: 0 };
}
exports.TimePoint_RenderFrame = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.group !== "") {
            writer.uint32(10).string(message.group);
        }
        if (message.frame !== 0) {
            writer.uint32(16).uint64(message.frame);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseTimePoint_RenderFrame();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.group = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.frame = longToNumber(reader.uint64());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            group: isSet(object.group) ? globalThis.String(object.group) : "",
            frame: isSet(object.frame) ? globalThis.Number(object.frame) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.group !== "") {
            obj.group = message.group;
        }
        if (message.frame !== 0) {
            obj.frame = Math.round(message.frame);
        }
        return obj;
    },
    create(base) {
        return exports.TimePoint_RenderFrame.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseTimePoint_RenderFrame();
        message.group = object.group ?? "";
        message.frame = object.frame ?? 0;
        return message;
    },
};
function createBaseSignalPacket() {
    return {
        schema: "",
        source: undefined,
        time: undefined,
        priority: 0,
        state: undefined,
        event: undefined,
        bundle: undefined,
    };
}
exports.SignalPacket = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.schema !== "") {
            writer.uint32(10).string(message.schema);
        }
        if (message.source !== undefined) {
            exports.Source.encode(message.source, writer.uint32(18).fork()).join();
        }
        if (message.time !== undefined) {
            exports.TimePoint.encode(message.time, writer.uint32(26).fork()).join();
        }
        if (message.priority !== 0) {
            writer.uint32(32).uint32(message.priority);
        }
        if (message.state !== undefined) {
            exports.State.encode(message.state, writer.uint32(82).fork()).join();
        }
        if (message.event !== undefined) {
            exports.Event.encode(message.event, writer.uint32(90).fork()).join();
        }
        if (message.bundle !== undefined) {
            exports.Bundle.encode(message.bundle, writer.uint32(98).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSignalPacket();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.schema = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.source = exports.Source.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.time = exports.TimePoint.decode(reader, reader.uint32());
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.priority = reader.uint32();
                    continue;
                }
                case 10: {
                    if (tag !== 82) {
                        break;
                    }
                    message.state = exports.State.decode(reader, reader.uint32());
                    continue;
                }
                case 11: {
                    if (tag !== 90) {
                        break;
                    }
                    message.event = exports.Event.decode(reader, reader.uint32());
                    continue;
                }
                case 12: {
                    if (tag !== 98) {
                        break;
                    }
                    message.bundle = exports.Bundle.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            schema: isSet(object.schema) ? globalThis.String(object.schema) : "",
            source: isSet(object.source) ? exports.Source.fromJSON(object.source) : undefined,
            time: isSet(object.time) ? exports.TimePoint.fromJSON(object.time) : undefined,
            priority: isSet(object.priority) ? globalThis.Number(object.priority) : 0,
            state: isSet(object.state) ? exports.State.fromJSON(object.state) : undefined,
            event: isSet(object.event) ? exports.Event.fromJSON(object.event) : undefined,
            bundle: isSet(object.bundle) ? exports.Bundle.fromJSON(object.bundle) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.schema !== "") {
            obj.schema = message.schema;
        }
        if (message.source !== undefined) {
            obj.source = exports.Source.toJSON(message.source);
        }
        if (message.time !== undefined) {
            obj.time = exports.TimePoint.toJSON(message.time);
        }
        if (message.priority !== 0) {
            obj.priority = Math.round(message.priority);
        }
        if (message.state !== undefined) {
            obj.state = exports.State.toJSON(message.state);
        }
        if (message.event !== undefined) {
            obj.event = exports.Event.toJSON(message.event);
        }
        if (message.bundle !== undefined) {
            obj.bundle = exports.Bundle.toJSON(message.bundle);
        }
        return obj;
    },
    create(base) {
        return exports.SignalPacket.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSignalPacket();
        message.schema = object.schema ?? "";
        message.source = (object.source !== undefined && object.source !== null)
            ? exports.Source.fromPartial(object.source)
            : undefined;
        message.time = (object.time !== undefined && object.time !== null) ? exports.TimePoint.fromPartial(object.time) : undefined;
        message.priority = object.priority ?? 0;
        message.state = (object.state !== undefined && object.state !== null) ? exports.State.fromPartial(object.state) : undefined;
        message.event = (object.event !== undefined && object.event !== null) ? exports.Event.fromPartial(object.event) : undefined;
        message.bundle = (object.bundle !== undefined && object.bundle !== null)
            ? exports.Bundle.fromPartial(object.bundle)
            : undefined;
        return message;
    },
};
function createBaseState() {
    return { path: "", value: undefined, validUntil: undefined, release: false };
}
exports.State = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.path !== "") {
            writer.uint32(10).string(message.path);
        }
        if (message.value !== undefined) {
            common_1.Value.encode(message.value, writer.uint32(18).fork()).join();
        }
        if (message.validUntil !== undefined) {
            exports.TimePoint.encode(message.validUntil, writer.uint32(26).fork()).join();
        }
        if (message.release !== false) {
            writer.uint32(32).bool(message.release);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseState();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.path = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.value = common_1.Value.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.validUntil = exports.TimePoint.decode(reader, reader.uint32());
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.release = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            path: isSet(object.path) ? globalThis.String(object.path) : "",
            value: isSet(object.value) ? common_1.Value.fromJSON(object.value) : undefined,
            validUntil: isSet(object.validUntil)
                ? exports.TimePoint.fromJSON(object.validUntil)
                : isSet(object.valid_until)
                    ? exports.TimePoint.fromJSON(object.valid_until)
                    : undefined,
            release: isSet(object.release) ? globalThis.Boolean(object.release) : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.path !== "") {
            obj.path = message.path;
        }
        if (message.value !== undefined) {
            obj.value = common_1.Value.toJSON(message.value);
        }
        if (message.validUntil !== undefined) {
            obj.validUntil = exports.TimePoint.toJSON(message.validUntil);
        }
        if (message.release !== false) {
            obj.release = message.release;
        }
        return obj;
    },
    create(base) {
        return exports.State.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseState();
        message.path = object.path ?? "";
        message.value = (object.value !== undefined && object.value !== null) ? common_1.Value.fromPartial(object.value) : undefined;
        message.validUntil = (object.validUntil !== undefined && object.validUntil !== null)
            ? exports.TimePoint.fromPartial(object.validUntil)
            : undefined;
        message.release = object.release ?? false;
        return message;
    },
};
function createBaseEvent() {
    return { path: "", payload: undefined, dedupeKey: undefined, targetTime: undefined };
}
exports.Event = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.path !== "") {
            writer.uint32(10).string(message.path);
        }
        if (message.payload !== undefined) {
            common_1.Value.encode(message.payload, writer.uint32(18).fork()).join();
        }
        if (message.dedupeKey !== undefined) {
            writer.uint32(26).string(message.dedupeKey);
        }
        if (message.targetTime !== undefined) {
            exports.TimePoint.encode(message.targetTime, writer.uint32(34).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseEvent();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.path = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.payload = common_1.Value.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.dedupeKey = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.targetTime = exports.TimePoint.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            path: isSet(object.path) ? globalThis.String(object.path) : "",
            payload: isSet(object.payload) ? common_1.Value.fromJSON(object.payload) : undefined,
            dedupeKey: isSet(object.dedupeKey)
                ? globalThis.String(object.dedupeKey)
                : isSet(object.dedupe_key)
                    ? globalThis.String(object.dedupe_key)
                    : undefined,
            targetTime: isSet(object.targetTime)
                ? exports.TimePoint.fromJSON(object.targetTime)
                : isSet(object.target_time)
                    ? exports.TimePoint.fromJSON(object.target_time)
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.path !== "") {
            obj.path = message.path;
        }
        if (message.payload !== undefined) {
            obj.payload = common_1.Value.toJSON(message.payload);
        }
        if (message.dedupeKey !== undefined) {
            obj.dedupeKey = message.dedupeKey;
        }
        if (message.targetTime !== undefined) {
            obj.targetTime = exports.TimePoint.toJSON(message.targetTime);
        }
        return obj;
    },
    create(base) {
        return exports.Event.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseEvent();
        message.path = object.path ?? "";
        message.payload = (object.payload !== undefined && object.payload !== null)
            ? common_1.Value.fromPartial(object.payload)
            : undefined;
        message.dedupeKey = object.dedupeKey ?? undefined;
        message.targetTime = (object.targetTime !== undefined && object.targetTime !== null)
            ? exports.TimePoint.fromPartial(object.targetTime)
            : undefined;
        return message;
    },
};
function createBaseBundle() {
    return { items: [], targetTime: undefined, atomic: false };
}
exports.Bundle = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.items) {
            exports.Bundle_Item.encode(v, writer.uint32(10).fork()).join();
        }
        if (message.targetTime !== undefined) {
            exports.TimePoint.encode(message.targetTime, writer.uint32(18).fork()).join();
        }
        if (message.atomic !== false) {
            writer.uint32(24).bool(message.atomic);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseBundle();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.items.push(exports.Bundle_Item.decode(reader, reader.uint32()));
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.targetTime = exports.TimePoint.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.atomic = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            items: globalThis.Array.isArray(object?.items) ? object.items.map((e) => exports.Bundle_Item.fromJSON(e)) : [],
            targetTime: isSet(object.targetTime)
                ? exports.TimePoint.fromJSON(object.targetTime)
                : isSet(object.target_time)
                    ? exports.TimePoint.fromJSON(object.target_time)
                    : undefined,
            atomic: isSet(object.atomic) ? globalThis.Boolean(object.atomic) : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.items?.length) {
            obj.items = message.items.map((e) => exports.Bundle_Item.toJSON(e));
        }
        if (message.targetTime !== undefined) {
            obj.targetTime = exports.TimePoint.toJSON(message.targetTime);
        }
        if (message.atomic !== false) {
            obj.atomic = message.atomic;
        }
        return obj;
    },
    create(base) {
        return exports.Bundle.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseBundle();
        message.items = object.items?.map((e) => exports.Bundle_Item.fromPartial(e)) || [];
        message.targetTime = (object.targetTime !== undefined && object.targetTime !== null)
            ? exports.TimePoint.fromPartial(object.targetTime)
            : undefined;
        message.atomic = object.atomic ?? false;
        return message;
    },
};
function createBaseBundle_Item() {
    return { state: undefined, event: undefined };
}
exports.Bundle_Item = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.state !== undefined) {
            exports.State.encode(message.state, writer.uint32(10).fork()).join();
        }
        if (message.event !== undefined) {
            exports.Event.encode(message.event, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseBundle_Item();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.state = exports.State.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.event = exports.Event.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            state: isSet(object.state) ? exports.State.fromJSON(object.state) : undefined,
            event: isSet(object.event) ? exports.Event.fromJSON(object.event) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.state !== undefined) {
            obj.state = exports.State.toJSON(message.state);
        }
        if (message.event !== undefined) {
            obj.event = exports.Event.toJSON(message.event);
        }
        return obj;
    },
    create(base) {
        return exports.Bundle_Item.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseBundle_Item();
        message.state = (object.state !== undefined && object.state !== null) ? exports.State.fromPartial(object.state) : undefined;
        message.event = (object.event !== undefined && object.event !== null) ? exports.Event.fromPartial(object.event) : undefined;
        return message;
    },
};
function bytesFromBase64(b64) {
    if (globalThis.Buffer) {
        return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
    }
    else {
        const bin = globalThis.atob(b64);
        const arr = new Uint8Array(bin.length);
        for (let i = 0; i < bin.length; ++i) {
            arr[i] = bin.charCodeAt(i);
        }
        return arr;
    }
}
function base64FromBytes(arr) {
    if (globalThis.Buffer) {
        return globalThis.Buffer.from(arr).toString("base64");
    }
    else {
        const bin = [];
        arr.forEach((byte) => {
            bin.push(globalThis.String.fromCharCode(byte));
        });
        return globalThis.btoa(bin.join(""));
    }
}
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
