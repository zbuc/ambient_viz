{
  "name": "ambient_viz-server",
  "version": "0.1.0",
  "private": true,
  "description": "Serves the static visualizer and streams Pi GPIO inputs to the browser over SSE.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.12.0",
    "serialport": "^12.0.0"
  }
}
